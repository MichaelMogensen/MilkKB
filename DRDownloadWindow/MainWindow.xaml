<Window x:Class="DRDownloadWindow.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DRDownloadWindow"
        mc:Ignorable="d"
        Title="DR Downloader" Height="800" Width="1200">
    <Window.CommandBindings>
        <CommandBinding Command="NavigationCommands.GoToPage" CanExecute="OnGoToPageCanExecute" Executed="OnGoToPageExecuted" />
    </Window.CommandBindings>
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="True">

            <Button DockPanel.Dock="Right" Command="NavigationCommands.GoToPage">
                <Image Source="C:\Users\micha\source\repos\MilkKB\DRDownloadWindow\Ressources\Png\go.png" Width="50" Height="50"/>
            </Button>
            <Separator DockPanel.Dock="Right" />

            <TextBox Name="urlTextBox" VerticalContentAlignment="Center" KeyUp="OnUrlEnter" FontSize="30" Background="LightSalmon"/>

        </DockPanel>

        <DockPanel Grid.Row="1" LastChildFill="True">

            <Grid DockPanel.Dock="Top">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="25*"/>
                    <RowDefinition Height="75*"/>
                </Grid.RowDefinitions>

                <TextBox Grid.Row="0" Name="rawHtmlTextBox" FontSize="10" Background="AntiqueWhite" MinHeight="200" Text="Raw html" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap"/>
                <TextBox Grid.Row="1" Name="broadcastRecordTextBox" FontSize="20" Background="Beige" Text="Broadcast record" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap"/>

            </Grid>

        </DockPanel>

    </Grid>
</Window>
