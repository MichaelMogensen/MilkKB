<Window x:Class="DRDownloadWindow.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DRDownloadWindow.ViewModels"
        xmlns:uc="clr-namespace:DRDownloadWindow.UserControls"
        mc:Ignorable="d"
        Background="PeachPuff" 
        Height="800" 
        Width="1200"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        Topmost="False"
        Closing="OnWindowClosing"
        MouseLeftButtonDown="OnLeftMouseButtonDown"
        xmlns:utilities="clr-namespace:DRDownloadWindow.Converters"
        d:DataContext="{d:DesignInstance local:BroadcastViewModel, IsDesignTimeCreatable=False}">
    <Border Background="BlanchedAlmond" CornerRadius="30" Margin="20" Padding="30">

        <Border.Resources>
            <utilities:CanDownloadConverter x:Key="canDownloadConverter"/>
        </Border.Resources>

        <DockPanel LastChildFill="False">

            <!-- Logo -->
            <Image DockPanel.Dock="Top" Width="400" Margin="0,0,0,0" Source="./Ressources/Images/dr_no_ia.png" />

            <!-- Metadata -->
            <StackPanel DockPanel.Dock="Top" Orientation="Vertical" Margin="0,0,0,30" >
                <TextBox Text="{Binding Path=Title}" FontFamily="Reem Kufi" Background="BlanchedAlmond" Foreground="Gray" BorderThickness="0" FontSize="30" IsReadOnly="True" FontWeight="Bold"></TextBox>
                <Separator Background="Gray"/>
                <TextBox Text="{Binding Path=SendDateAndDuration}" FontFamily="Reem Kufi" Background="BlanchedAlmond" Foreground="Gray" BorderThickness="0" FontSize="20" IsReadOnly="True"></TextBox>
                <ScrollViewer VerticalScrollBarVisibility="Auto" Height="100">
                    <TextBlock Text="{Binding Path=Description}" TextWrapping="WrapWithOverflow" FontFamily="Reem Kufi" Background="BlanchedAlmond" Foreground="Gray" FontSize="20"></TextBlock>
                </ScrollViewer>
                <TextBox Text="{Binding Path=Episode}" FontFamily="Reem Kufi" Background="BlanchedAlmond" Foreground="Gray" BorderThickness="0" FontSize="20" IsReadOnly="True"></TextBox>
                <TextBox Text="{Binding Path=Channel}" FontFamily="Reem Kufi" Background="BlanchedAlmond" Foreground="Gray" BorderThickness="0" FontSize="20" IsReadOnly="True"></TextBox>
                <TextBox Text="{Binding Path=Genre}" FontFamily="Reem Kufi" Background="BlanchedAlmond" Foreground="Gray" BorderThickness="0" FontSize="20" IsReadOnly="True"></TextBox>

                <!-- Tekniske dataljer -->
                <Expander FontFamily="Reem Kufi" Background="BlanchedAlmond" FontSize="20" Foreground="Gray" HorizontalAlignment="Stretch" Header="Tekniske detaljer" ExpandDirection="Down" IsExpanded="False" Expanded="OnTechnicalDetailsExpanded" Collapsed="OnTechnicalDetailsCollapsed" >
                    <StackPanel Margin="0,10,0,0">
                        <CheckBox IsChecked="{Binding Path=GenerateLogFile, Mode=TwoWay}" FontFamily="Reem Kufi" FontSize="10" Command="{Binding GenerateLogFileCommand}">
                            Skriv logfil
                        </CheckBox>

                        <CheckBox IsChecked="{Binding Path=DeleteM3uFileAfterDownload, Mode=TwoWay}" FontFamily="Reem Kufi" FontSize="10" Command="{Binding DeleteM3uFileAfterDownloadCommand}" Margin="0,0,0,10">
                            Slet m3u-fil efter download
                        </CheckBox>

                        <TextBox Text="{Binding Path=UniqueId}" FontFamily="Reem Kufi" Background="BlanchedAlmond" Foreground="Gray" BorderThickness="0" FontSize="10" IsReadOnly="True"></TextBox>
                        <TextBox Text="{Binding Path=EntryId}" FontFamily="Reem Kufi" Background="BlanchedAlmond" Foreground="Gray" BorderThickness="0" FontSize="10" IsReadOnly="True"></TextBox>
                        <TextBox Text="{Binding Path=MediaType}" FontFamily="Reem Kufi" Background="BlanchedAlmond" Foreground="Gray" BorderThickness="0" FontSize="10" IsReadOnly="True"></TextBox>
                        <TextBox Text="{Binding Path=Url}" FontFamily="Reem Kufi" Background="BlanchedAlmond" Foreground="Gray" BorderThickness="0" FontSize="10" IsReadOnly="True"></TextBox>
                        <TextBox Text="{Binding Path=DownloadFolder}" FontFamily="Reem Kufi" Background="BlanchedAlmond" Foreground="Gray" BorderThickness="0" FontSize="10" IsReadOnly="True"></TextBox>
                        <TextBox Text="{Binding Path=Mp3File}" FontFamily="Reem Kufi" Background="BlanchedAlmond" Foreground="Gray" BorderThickness="0" FontSize="10" IsReadOnly="True"></TextBox>
                        <TextBox Text="{Binding Path=M3uFile}" FontFamily="Reem Kufi" Background="BlanchedAlmond" Foreground="Gray" BorderThickness="0" FontSize="10" IsReadOnly="True"></TextBox>
                        <TextBox Text="{Binding Path=Mp4File}" FontFamily="Reem Kufi" Background="BlanchedAlmond" Foreground="Gray" BorderThickness="0" FontSize="10" IsReadOnly="True"></TextBox>
                        <TextBox Text="{Binding Path=LogFile}" FontFamily="Reem Kufi" Background="BlanchedAlmond" Foreground="Gray" BorderThickness="0" FontSize="10" IsReadOnly="True"></TextBox>
                    </StackPanel>
                </Expander>
            </StackPanel>

            <!-- Statusbar -->
            <DockPanel DockPanel.Dock="Bottom" LastChildFill="False" Margin="0,30,0,0">
                <StatusBar DockPanel.Dock="Left" Background="BlanchedAlmond" Foreground="Gray">
                    <StatusBarItem FontFamily="Reem Kufi">
                        <TextBox Text="{Binding Path=StatusBar}" FontFamily="Reem Kufi" Background="BlanchedAlmond" Foreground="{Binding Path=StatusBarColor}" BorderThickness="0" FontSize="10" IsReadOnly="True"></TextBox>
                    </StatusBarItem>
                </StatusBar>
            </DockPanel>

            <!-- Buttons -->
            <Grid Name="gridButtons" DockPanel.Dock="Bottom" HorizontalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <!-- Copy -->
                <Button Grid.Row="0" Grid.Column="0" Width="100" Height="100" Command="{Binding CopyCommand}">
                    <Image Source="./Ressources/Images/copy.png"/>
                </Button>

                <!-- DownloadButton 0 -->
                <uc:ProgressButton Grid.Row="0" Grid.Column="1" Value="{Binding Path=Progressbar0}" Command="{Binding Path=DownloadCommand0}" ToolTip="Hent radio/tv udsendelse">
                    <uc:ProgressButton.IsEnabled>
                        <MultiBinding Converter="{StaticResource canDownloadConverter}">
                            <Binding Path="Progressbar0" Mode="OneWay"></Binding>
                            <Binding Path="EntryId" Mode="OneWay"></Binding>
                        </MultiBinding>
                    </uc:ProgressButton.IsEnabled>
                </uc:ProgressButton>

                <!-- DownloadButton 1 -->
                <uc:ProgressButton Grid.Row="0" Grid.Column="2" Value="{Binding Path=Progressbar1}" Command="{Binding Path=DownloadCommand1}" ToolTip="Hent radio/tv udsendelse">
                    <uc:ProgressButton.IsEnabled>
                        <MultiBinding Converter="{StaticResource canDownloadConverter}">
                            <Binding Path="Progressbar1" Mode="OneWay"></Binding>
                            <Binding Path="EntryId" Mode="OneWay"></Binding>
                        </MultiBinding>
                    </uc:ProgressButton.IsEnabled>
                </uc:ProgressButton>

                <!-- DownloadButton 2 -->
                <uc:ProgressButton Grid.Row="0" Grid.Column="3" Value="{Binding Path=Progressbar2}" Command="{Binding Path=DownloadCommand2}" ToolTip="Hent radio/tv udsendelse">
                    <uc:ProgressButton.IsEnabled>
                        <MultiBinding Converter="{StaticResource canDownloadConverter}">
                            <Binding Path="Progressbar2" Mode="OneWay"></Binding>
                            <Binding Path="EntryId" Mode="OneWay"></Binding>
                        </MultiBinding>
                    </uc:ProgressButton.IsEnabled>
                </uc:ProgressButton>

                <!-- DownloadButton 3 -->
                <uc:ProgressButton Grid.Row="0" Grid.Column="4" Value="{Binding Path=Progressbar3}" Command="{Binding Path=DownloadCommand3}" ToolTip="Hent radio/tv udsendelse">
                    <uc:ProgressButton.IsEnabled>
                        <MultiBinding Converter="{StaticResource canDownloadConverter}">
                            <Binding Path="Progressbar3" Mode="OneWay"></Binding>
                            <Binding Path="EntryId" Mode="OneWay"></Binding>
                        </MultiBinding>
                    </uc:ProgressButton.IsEnabled>
                </uc:ProgressButton>

                <!-- DownloadButton 4 -->
                <uc:ProgressButton Grid.Row="0" Grid.Column="5" Value="{Binding Path=Progressbar4}" Command="{Binding Path=DownloadCommand4}" ToolTip="Hent radio/tv udsendelse">
                    <uc:ProgressButton.IsEnabled>
                        <MultiBinding Converter="{StaticResource canDownloadConverter}">
                            <Binding Path="Progressbar4" Mode="OneWay"></Binding>
                            <Binding Path="EntryId" Mode="OneWay"></Binding>
                        </MultiBinding>
                    </uc:ProgressButton.IsEnabled>
                </uc:ProgressButton>

                <!-- DownloadButton 5 -->
                <uc:ProgressButton Grid.Row="1" Grid.Column="1" Value="{Binding Path=Progressbar5}" Command="{Binding Path=DownloadCommand5}" ToolTip="Hent radio/tv udsendelse">
                    <uc:ProgressButton.IsEnabled>
                        <MultiBinding Converter="{StaticResource canDownloadConverter}">
                            <Binding Path="Progressbar5" Mode="OneWay"></Binding>
                            <Binding Path="EntryId" Mode="OneWay"></Binding>
                        </MultiBinding>
                    </uc:ProgressButton.IsEnabled>
                </uc:ProgressButton>

                <!-- DownloadButton 6 -->
                <uc:ProgressButton Grid.Row="1" Grid.Column="2" Value="{Binding Path=Progressbar6}" Command="{Binding Path=DownloadCommand6}" ToolTip="Hent radio/tv udsendelse">
                    <uc:ProgressButton.IsEnabled>
                        <MultiBinding Converter="{StaticResource canDownloadConverter}">
                            <Binding Path="Progressbar6" Mode="OneWay"></Binding>
                            <Binding Path="EntryId" Mode="OneWay"></Binding>
                        </MultiBinding>
                    </uc:ProgressButton.IsEnabled>
                </uc:ProgressButton>

                <!-- DownloadButton 7 -->
                <uc:ProgressButton Grid.Row="1" Grid.Column="3" Value="{Binding Path=Progressbar7}" Command="{Binding Path=DownloadCommand7}" ToolTip="Hent radio/tv udsendelse">
                    <uc:ProgressButton.IsEnabled>
                        <MultiBinding Converter="{StaticResource canDownloadConverter}">
                            <Binding Path="Progressbar7" Mode="OneWay"></Binding>
                            <Binding Path="EntryId" Mode="OneWay"></Binding>
                        </MultiBinding>
                    </uc:ProgressButton.IsEnabled>
                </uc:ProgressButton>

                <!-- DownloadButton 8 -->
                <uc:ProgressButton Grid.Row="1" Grid.Column="4" Value="{Binding Path=Progressbar8}" Command="{Binding Path=DownloadCommand8}" ToolTip="Hent radio/tv udsendelse">
                    <uc:ProgressButton.IsEnabled>
                        <MultiBinding Converter="{StaticResource canDownloadConverter}">
                            <Binding Path="Progressbar8" Mode="OneWay"></Binding>
                            <Binding Path="EntryId" Mode="OneWay"></Binding>
                        </MultiBinding>
                    </uc:ProgressButton.IsEnabled>
                </uc:ProgressButton>

                <!-- DownloadButton 9 -->
                <uc:ProgressButton Grid.Row="1" Grid.Column="5" Value="{Binding Path=Progressbar9}" Command="{Binding Path=DownloadCommand9}" ToolTip="Hent radio/tv udsendelse">
                    <uc:ProgressButton.IsEnabled>
                        <MultiBinding Converter="{StaticResource canDownloadConverter}">
                            <Binding Path="Progressbar9" Mode="OneWay"></Binding>
                            <Binding Path="EntryId" Mode="OneWay"></Binding>
                        </MultiBinding>
                    </uc:ProgressButton.IsEnabled>
                </uc:ProgressButton>

                <!-- Close -->
                <Button Grid.Row="2" Grid.Column="6" Width="100" Height="100" Click="OnBtnClose">
                    <Image Source="./Ressources/Images/close.png"/>
                </Button>

            </Grid>

        </DockPanel>

    </Border>
</Window>
